# A sample Guardfile
# More info at https://github.com/guard/guard#readme

# Add files and commands to this file, like the example:
#   watch('file/path') { `command(s)` }
#
guard 'shell' do
  watch('src/(.*).js') { `./build.sh; cp triggerFinger.js /Users/jcantrell/FeDev/cm/` }
end
